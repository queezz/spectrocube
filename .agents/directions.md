# Directions

Forward-looking work only.

## Current

No SpectroCube package work is queued after the 0.2.0 optional recalibration
contract. The next Echelle campaign packet belongs in `echelle_spectra` and
its external workflow repository; it should consume this neutral contract
without adding instrument logic here.
